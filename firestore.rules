rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Grant global unrestricted access as requested by the user to ensure everything works
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
